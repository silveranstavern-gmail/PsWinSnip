<UserControl x:Class="PsWinSnip.Controls.AspectRatioRect"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style x:Key="ResizeHandle" TargetType="Thumb">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Height" Value="8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <!-- The main selection border -->
        <Border x:Name="SelectionBorder"
                BorderBrush="White"
                BorderThickness="1"
                Background="#01FFFFFF"
                Cursor="Hand">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" ShadowDepth="0" Color="Black" Opacity="0.5"/>
            </Border.Effect>

            <Grid>
                <Grid x:Name="GuidesGrid" Visibility="Visible" Opacity="0.4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Vertical lines -->
                    <Rectangle Grid.Column="0" Grid.RowSpan="3" Stroke="White" StrokeThickness="0.5" HorizontalAlignment="Right" Width="1" Margin="0,0,-0.5,0"/>
                    <Rectangle Grid.Column="1" Grid.RowSpan="3" Stroke="White" StrokeThickness="0.5" HorizontalAlignment="Right" Width="1" Margin="0,0,-0.5,0"/>
                    
                    <!-- Horizontal lines -->
                    <Rectangle Grid.Row="0" Grid.ColumnSpan="3" Stroke="White" StrokeThickness="0.5" VerticalAlignment="Bottom" Height="1" Margin="0,0,0,-0.5"/>
                    <Rectangle Grid.Row="1" Grid.ColumnSpan="3" Stroke="White" StrokeThickness="0.5" VerticalAlignment="Bottom" Height="1" Margin="0,0,0,-0.5"/>
                </Grid>
                
                <Rectangle x:Name="DashedBorder" Margin="-1">
                    <Rectangle.Stroke>
                        <SolidColorBrush Color="Black"/>
                    </Rectangle.Stroke>
                    <Rectangle.StrokeThickness>1</Rectangle.StrokeThickness>
                    <Rectangle.StrokeDashArray>4 4</Rectangle.StrokeDashArray>
                </Rectangle>
            </Grid>
        </Border>

        <!-- Dimension Label -->
        <Border Background="#CC000000" CornerRadius="2" Padding="4,2" 
                HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,10">
            <TextBlock x:Name="DimensionsText" Foreground="White" FontSize="11" Text="0 x 0"/>
        </Border>

        <!-- Resize Handles -->
        <Canvas IsHitTestVisible="True">
            <!-- Corners -->
            <Thumb Style="{StaticResource ResizeHandle}" x:Name="HandleTopLeft" Cursor="SizeNWSE" Canvas.Left="-4" Canvas.Top="-4" DragDelta="OnResizeDelta" Tag="TopLeft"/>
            <Thumb Style="{StaticResource ResizeHandle}" x:Name="HandleTopRight" Cursor="SizeNESW" Canvas.Right="-4" Canvas.Top="-4" DragDelta="OnResizeDelta" Tag="TopRight"/>
            <Thumb Style="{StaticResource ResizeHandle}" x:Name="HandleBottomLeft" Cursor="SizeNESW" Canvas.Left="-4" Canvas.Bottom="-4" DragDelta="OnResizeDelta" Tag="BottomLeft"/>
            <Thumb Style="{StaticResource ResizeHandle}" x:Name="HandleBottomRight" Cursor="SizeNWSE" Canvas.Right="-4" Canvas.Bottom="-4" DragDelta="OnResizeDelta" Tag="BottomRight"/>

            <!-- Edges -->
            <Thumb Style="{StaticResource ResizeHandle}" x:Name="HandleTop" Cursor="SizeNS" Canvas.Left="0" Canvas.Top="-4" Width="Auto" HorizontalAlignment="Stretch" DragDelta="OnResizeDelta" Tag="Top"/>
            <Thumb Style="{StaticResource ResizeHandle}" x:Name="HandleBottom" Cursor="SizeNS" Canvas.Left="0" Canvas.Bottom="-4" Width="Auto" HorizontalAlignment="Stretch" DragDelta="OnResizeDelta" Tag="Bottom"/>
            <Thumb Style="{StaticResource ResizeHandle}" x:Name="HandleLeft" Cursor="SizeWE" Canvas.Left="-4" Canvas.Top="0" Height="Auto" VerticalAlignment="Stretch" DragDelta="OnResizeDelta" Tag="Left"/>
            <Thumb Style="{StaticResource ResizeHandle}" x:Name="HandleRight" Cursor="SizeWE" Canvas.Right="-4" Canvas.Top="0" Height="Auto" VerticalAlignment="Stretch" DragDelta="OnResizeDelta" Tag="Right"/>
        </Canvas>
    </Grid>
</UserControl>
